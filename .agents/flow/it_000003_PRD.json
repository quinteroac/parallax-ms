{
  "goals": [
    "Replace the stub inference path with a working `comfy-diffusion` `txt2img` pipeline.",
    "Write generated images to disk and serve them via a static URL included in the gateway callback.",
    "Surface worker errors (runtime failure, model load failure, inference error) as `failed` job state, visible via SSE and `GET /v1/job/:id`.",
    "Provide a minimal browser-based playground for manual end-to-end testing.",
    "Validate the full pipeline with an automated end-to-end test."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Validated infer payload",
      "description": "As a client application, I want the worker to validate the `POST /infer` request body so that malformed requests are rejected immediately with a clear error before inference is attempted.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "Worker defines a Pydantic model (e.g. `InferRequest`) with fields: `id` (str), `prompt` (str), `negative_prompt` (str, default `\"\"`), `width` (int, default 512), `height` (int, default 512), `steps` (int, default 20), `cfg` (float, default 7.0), `seed` (int, default 0), `sampler_name` (str, default `\"euler\"`), `scheduler` (str, default `\"normal\"`)."
        },
        {
          "id": "US-001-AC02",
          "text": "A request missing the required `id` or `prompt` fields returns **422 Unprocessable Entity**."
        },
        {
          "id": "US-001-AC03",
          "text": "A valid request returns **202 Accepted** as before."
        },
        {
          "id": "US-001-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Runtime bootstrap and model load at startup",
      "description": "As a worker process, I want to call `check_runtime()` and load the hardcoded checkpoint on startup so that inference requests can be served without cold-loading the model on the first request.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "Worker calls `comfy_diffusion.check_runtime()` during application startup (e.g. FastAPI lifespan handler)."
        },
        {
          "id": "US-002-AC02",
          "text": "If `check_runtime()` returns an `\"error\"` key, the worker logs the error and exits with a non-zero code."
        },
        {
          "id": "US-002-AC03",
          "text": "`ModelManager` is instantiated with `MODELS_DIR` (from env var, see FR-3) and `load_checkpoint(CHECKPOINT_FILENAME)` is called once at startup."
        },
        {
          "id": "US-002-AC04",
          "text": "If the checkpoint file does not exist or fails to load, the worker logs the error and exits with a non-zero code."
        },
        {
          "id": "US-002-AC05",
          "text": "`GET /health` returns **200 OK** only after the runtime and model are loaded successfully."
        },
        {
          "id": "US-002-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "txt2img inference writes artifact to disk",
      "description": "As a client application, I want the worker to run real `txt2img` inference so that a generated image is available on disk after the job completes.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "Background task runs the full pipeline: `encode_prompt` (positive + negative) → `empty_latent_image` → `sample` → `vae_decode` → save PIL `Image` as PNG to `OUTPUT_DIR/{job_id}.png` (see FR-5)."
        },
        {
          "id": "US-003-AC02",
          "text": "`sample()` uses `sampler_name`, `scheduler`, `steps`, `cfg`, and `seed` from the validated request."
        },
        {
          "id": "US-003-AC03",
          "text": "The output file exists on disk after the background task completes."
        },
        {
          "id": "US-003-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Generated image served and URL included in callback",
      "description": "As a client application, I want the callback from the worker to include a URL pointing to the generated image so that I can fetch the result after receiving the SSE completion event.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "Worker serves output images as static files (see FR-6)."
        },
        {
          "id": "US-004-AC02",
          "text": "After saving the image, the worker POSTs `{ \"id\": job_id, \"url\": \"<served image URL>\" }` to the gateway's `/worker/done` endpoint."
        },
        {
          "id": "US-004-AC03",
          "text": "`GET /v1/job/:id` on the gateway returns `{ \"status\": \"succeeded\", \"url\": \"<image URL>\" }` after the callback is received."
        },
        {
          "id": "US-004-AC04",
          "text": "The image URL is reachable via HTTP and returns the PNG file."
        },
        {
          "id": "US-004-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Worker errors propagate as failed job state",
      "description": "As a client application, I want inference errors to be surfaced as a `failed` job status so that I am not left waiting forever on a job that will never complete.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "If inference throws an exception, the worker POSTs `{ \"id\": job_id, \"error\": \"<message>\" }` to `/worker/done` (or a dedicated error callback — see FR-7)."
        },
        {
          "id": "US-005-AC02",
          "text": "Gateway updates the job state to `failed` and emits an SSE event (e.g. `{ \"status\": \"failed\", \"error\": \"...\" }`)."
        },
        {
          "id": "US-005-AC03",
          "text": "`GET /v1/job/:id` returns `{ \"status\": \"failed\", \"error\": \"<message>\" }`."
        },
        {
          "id": "US-005-AC04",
          "text": "No silent hangs: if the background task errors, the callback is always sent."
        },
        {
          "id": "US-005-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "Basic playground page for manual testing",
      "description": "As a developer, I want a simple browser UI so that I can submit a `txt2img` job, watch the SSE stream, and see the generated image without writing curl commands.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "Gateway serves a static HTML page at `GET /playground`."
        },
        {
          "id": "US-006-AC02",
          "text": "Page contains a form with fields: **Prompt** (text), **Negative prompt** (text, optional), **Steps** (number, default 20), **Seed** (number, default 0)."
        },
        {
          "id": "US-006-AC03",
          "text": "On submit, the page calls the gateway job creation endpoint, opens an SSE connection for the returned `jobId`, and displays a loading indicator."
        },
        {
          "id": "US-006-AC04",
          "text": "On SSE completion event, the page displays the generated image inline."
        },
        {
          "id": "US-006-AC05",
          "text": "On SSE error event, the page displays the error message."
        },
        {
          "id": "US-006-AC06",
          "text": "Visually verified in browser: form submits, image appears, error state is visible."
        },
        {
          "id": "US-006-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-007",
      "title": "Automated end-to-end test",
      "description": "As a developer, I want an automated test that exercises the full inference path so that regressions are caught without manual intervention.",
      "acceptanceCriteria": [
        {
          "id": "US-007-AC01",
          "text": "A pytest test (in `worker/tests/`) submits a minimal `txt2img` request directly to the worker's `POST /infer` endpoint (or via the full gateway → worker path if integration test)."
        },
        {
          "id": "US-007-AC02",
          "text": "Test asserts the output PNG file is created on disk within a reasonable timeout."
        },
        {
          "id": "US-007-AC03",
          "text": "Test asserts the callback payload contains a valid `url` field (can be verified against the static file path)."
        },
        {
          "id": "US-007-AC04",
          "text": "Test is skipped automatically if `MODELS_DIR` / `CHECKPOINT_FILENAME` env vars are not set (to allow CI runs without GPU/model files)."
        },
        {
          "id": "US-007-AC05",
          "text": "`uv run pytest` passes (or skips gracefully) in a clean environment."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** Worker defines a Pydantic `InferRequest` model with fields: `id`, `prompt`, `negative_prompt` (default `\"\"`), `width` (default 512), `height` (default 512), `steps` (default 20), `cfg` (default 7.0), `seed` (default 0), `sampler_name` (default `\"euler\"`), `scheduler` (default `\"normal\"`)."
    },
    {
      "id": "FR-2",
      "description": "** Worker calls `comfy_diffusion.check_runtime()` in the FastAPI lifespan handler before accepting traffic; exits non-zero on failure."
    },
    {
      "id": "FR-3",
      "description": "** Worker reads two env vars: `MODELS_DIR` (path to the models directory passed to `ModelManager`) and `CHECKPOINT_FILENAME` (filename under `models_dir/checkpoints/` passed to `load_checkpoint`). Both are required at startup; missing values cause startup failure with a clear log message."
    },
    {
      "id": "FR-4",
      "description": "** `txt2img` pipeline (called in background task): `encode_prompt(clip, prompt)` + `encode_prompt(clip, negative_prompt)` → `empty_latent_image(width, height)` → `sample(model, positive, negative, latent, steps, cfg, sampler_name, scheduler, seed)` → `vae_decode(vae, latent)` → save PIL Image as PNG."
    },
    {
      "id": "FR-5",
      "description": "** Output images are saved to `OUTPUT_DIR/{job_id}.png`. `OUTPUT_DIR` is configurable via env var (default: `./outputs`)."
    },
    {
      "id": "FR-6",
      "description": "** The **gateway** serves the output directory as static files (e.g. Elysia static mount). The served URL for a job is `<GATEWAY_BASE_URL>/outputs/{job_id}.png`. Worker and gateway must share (or mount) the same `OUTPUT_DIR` path so the gateway can serve files written by the worker."
    },
    {
      "id": "FR-7",
      "description": "** On any exception in the background task, the worker sends a completion callback with `{ \"id\": job_id, \"error\": \"<str(exception)>\" }`. Gateway maps this to `status: \"failed\"`."
    },
    {
      "id": "FR-8",
      "description": "** Gateway `/worker/done` handler accepts an optional `error` field in the body; if present, sets job `status` to `\"failed\"` and includes the error in the SSE event and `GET /v1/job/:id` response."
    },
    {
      "id": "FR-9",
      "description": "** Gateway serves a static HTML playground at `GET /playground` with the form and SSE client logic described in US-006."
    },
    {
      "id": "FR-10",
      "description": "** `MODELS_DIR`, `CHECKPOINT_FILENAME`, and `OUTPUT_DIR` are added to `worker/.env.example` as documented placeholders."
    }
  ]
}
