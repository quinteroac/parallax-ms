{
  "goals": [
    "Enforce `modelId` and `modality` as required, validated fields on `POST /v1/jobs`.",
    "Guarantee consistent `pending / running / succeeded / failed` semantics across the in-memory store, `GET /v1/jobs/:id`, and SSE responses.",
    "Expose an OpenAPI 3.x spec and Swagger UI at a documented URL (`/swagger`).",
    "Update README and AGENTS.md so the locked public contract is reflected in project documentation."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "`POST /v1/jobs` validates `modelId` and `modality`",
      "description": "As a client application, I want `POST /v1/jobs` to require and validate `modelId` and `modality` so that I receive a clear error immediately when I submit an unsupported or unknown combination instead of silently creating an invalid job.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "Request body shape: `{ modelId: string, modality: string, params: object }`. `modelId` and `modality` are required; `params` is required (may be an empty object)."
        },
        {
          "id": "US-001-AC02",
          "text": "If `modelId` is missing or not a non-empty string, returns HTTP 400 with `{ error: \"\\`modelId\\` is required and must be a non-empty string\" }`."
        },
        {
          "id": "US-001-AC03",
          "text": "If `modelId` does not match any model in the loaded `models.config.json`, returns HTTP 422 with `{ error: \"Model not found: <modelId>\" }`."
        },
        {
          "id": "US-001-AC04",
          "text": "If `modality` is missing or not a non-empty string, returns HTTP 400 with `{ error: \"\\`modality\\` is required and must be a non-empty string\" }`."
        },
        {
          "id": "US-001-AC05",
          "text": "If `modality` is not listed in the matched model's `modalities` array, returns HTTP 422 with `{ error: \"Modality '<modality>' is not supported by model '<modelId>'\" }`."
        },
        {
          "id": "US-001-AC06",
          "text": "On valid input, returns HTTP 201 with `{ jobId: string }` (unchanged behaviour)."
        },
        {
          "id": "US-001-AC07",
          "text": "The job stored in the job-store includes `modelId` and `modality` alongside `type` (derived from the model's `type` field) and `params`."
        },
        {
          "id": "US-001-AC08",
          "text": "If `models.config.json` is unavailable at request time, returns HTTP 503 with `{ error: \"Model configuration unavailable\" }`."
        },
        {
          "id": "US-001-AC09",
          "text": "Existing tests pass; new unit/integration tests cover all error branches above."
        },
        {
          "id": "US-001-AC10",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "`GET /v1/jobs/:id` response shape is documented and consistent",
      "description": "As a client application, I want `GET /v1/jobs/:id` to return a documented, stable response shape so that I can reliably map job state in my UI without guessing optional fields.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "Response always includes: `id` (string), `status` (`\"pending\" | \"running\" | \"succeeded\" | \"failed\"`), `createdAt` (ISO 8601 string), `updatedAt` (ISO 8601 string)."
        },
        {
          "id": "US-002-AC02",
          "text": "Response includes `modelId` (string) and `modality` (string) — both always present (no longer implicit)."
        },
        {
          "id": "US-002-AC03",
          "text": "`url` (string) is present only when `status === \"succeeded\"`."
        },
        {
          "id": "US-002-AC04",
          "text": "`error` (string) is present only when `status === \"failed\"`."
        },
        {
          "id": "US-002-AC05",
          "text": "Returns HTTP 404 with `{ error: \"Job not found\" }` for unknown IDs (unchanged)."
        },
        {
          "id": "US-002-AC06",
          "text": "The Elysia route handler uses a typed response schema (TypeBox `t.Object(...)`) matching the shape above so Swagger reflects it."
        },
        {
          "id": "US-002-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "OpenAPI spec and Swagger UI accessible at `/swagger`",
      "description": "As a client application developer, I want a Swagger UI available at `/swagger` and a raw OpenAPI JSON spec at `/swagger/json` so that I can explore and test all `/v1/...` endpoints interactively without reading source code.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`GET /swagger` returns an HTML Swagger UI page (HTTP 200)."
        },
        {
          "id": "US-003-AC02",
          "text": "`GET /swagger/json` returns a valid OpenAPI 3.x JSON document (HTTP 200)."
        },
        {
          "id": "US-003-AC03",
          "text": "The spec includes all `/v1/...` routes: `GET /v1/models`, `GET /v1/models/:id`, `POST /v1/jobs`, `GET /v1/jobs/:id`, `GET /v1/jobs/:id/events`."
        },
        {
          "id": "US-003-AC04",
          "text": "Each route has a non-empty `summary` and documented request/response schemas (no routes with empty or missing `detail`)."
        },
        {
          "id": "US-003-AC05",
          "text": "The spec is served by the existing Elysia app (via `@elysiajs/swagger` plugin or equivalent); no separate process is required."
        },
        {
          "id": "US-003-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "README and AGENTS.md reflect the locked public contract",
      "description": "As a developer or integrator, I want the README and AGENTS.md to describe the current versioned API surface so that the documentation is the authoritative reference for the job lifecycle and discovery endpoints.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "README contains an **API reference** section (or updates an existing one) listing: `POST /v1/jobs` (request body, response, error codes), `GET /v1/jobs/:id` (response shape, all four statuses), `GET /v1/jobs/:id/events` (SSE event format), `GET /v1/models`, `GET /v1/models/:id`, and `GET /swagger`."
        },
        {
          "id": "US-004-AC02",
          "text": "The README documents the four job statuses (`pending`, `running`, `succeeded`, `failed`) and when each field (`url`, `error`) is present."
        },
        {
          "id": "US-004-AC03",
          "text": "`AGENTS.md` or `PROJECT_CONTEXT.md` \"Implemented Capabilities\" section is updated to reflect iteration 000005 deliverables."
        },
        {
          "id": "US-004-AC04",
          "text": "No contradictions between README and current implementation."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `POST /v1/jobs` request body requires `modelId` (string), `modality` (string), and `params` (object). The `type` field is removed from the request body and is instead derived from the model's `type` in `models.config.json`."
    },
    {
      "id": "FR-2",
      "description": "** Validation order for `POST /v1/jobs`: (1) body shape check → 400, (2) model lookup → 422, (3) modality check → 422, (4) config unavailable → 503."
    },
    {
      "id": "FR-3",
      "description": "** The `Job` interface in `job-store.ts` gains `modelId: string` and `modality: string` fields; `type` is kept for internal reference."
    },
    {
      "id": "FR-4",
      "description": "** `GET /v1/jobs/:id` response always includes `modelId` and `modality`; `url` and `error` are omitted (not `null`) when not applicable."
    },
    {
      "id": "FR-5",
      "description": "** The Swagger plugin is registered on the root Elysia app in `index.ts` with `title: \"Parallax Media Server\"` and the current API version."
    },
    {
      "id": "FR-6",
      "description": "** All `/v1/...` route `detail.summary` strings are present and descriptive (non-empty)."
    },
    {
      "id": "FR-7",
      "description": "** README API reference section uses a consistent format: route, method, brief description, request schema, response schema, and error codes."
    }
  ]
}
