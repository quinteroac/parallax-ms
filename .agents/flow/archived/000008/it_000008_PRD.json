{
  "goals": [
    "Deliver `txt2vid` and `img2vid` inference end-to-end through the existing gateway → worker → artifact → callback → SSE path.",
    "Expose video models in the model discovery API with the correct modalities.",
    "Allow clients to control output resolution and duration via job parameters.",
    "Surface all failures clearly — no silent hangs or ambiguous success responses.",
    "Extend the playground UI to submit video jobs and display the resulting MP4."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Model config includes video model entries",
      "description": "As a client, I want `GET /v1/models` to return video-capable models tagged with `txt2vid` and/or `img2vid` modalities so that I can discover which models to target when submitting video jobs.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`models.config.json` contains at least one model entry with `modality: [\"txt2vid\"]` and at least one with `modality: [\"img2vid\"]` (may be the same model if it supports both)."
        },
        {
          "id": "US-001-AC02",
          "text": "`GET /v1/models` returns those entries with correct `id`, `architecture`, `modality`, and `components` fields."
        },
        {
          "id": "US-001-AC03",
          "text": "`GET /v1/models/:id` resolves each video model entry by its `id`."
        },
        {
          "id": "US-001-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Gateway accepts and validates video jobs",
      "description": "As a client, I want `POST /v1/jobs` to accept `txt2vid` and `img2vid` modalities with video-specific parameters so that I can enqueue video generation jobs without the gateway rejecting valid requests.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`POST /v1/jobs` with `modality: \"txt2vid\"` and a valid `modelId` returns `201` with a `jobId`."
        },
        {
          "id": "US-002-AC02",
          "text": "`POST /v1/jobs` with `modality: \"img2vid\"`, a valid `modelId`, and an `inputImage` field returns `201` with a `jobId`."
        },
        {
          "id": "US-002-AC03",
          "text": "Both requests accept `width`, `height`, and `duration` (in seconds or frames — document the unit) as optional numeric parameters."
        },
        {
          "id": "US-002-AC04",
          "text": "Submitting `modality: \"txt2vid\"` or `modality: \"img2vid\"` with a model that does not support those modalities returns `400` with a descriptive error message."
        },
        {
          "id": "US-002-AC05",
          "text": "Submitting `modality: \"img2vid\"` without an `inputImage` returns `400`."
        },
        {
          "id": "US-002-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Worker runs txt2vid inference end-to-end",
      "description": "As a client, I want a `txt2vid` job to run real video inference via `comfy_diffusion` and deliver a playable MP4 so that I receive a usable video artifact when the job completes.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "Worker `POST /infer` accepts `modality: \"txt2vid\"` with `prompt`, `modelId`, `width`, `height`, `duration`, and all required `comfy_diffusion` parameters."
        },
        {
          "id": "US-003-AC02",
          "text": "Inference runs asynchronously after `202 Accepted`; the worker uses `comfy_diffusion` exclusively (no other inference library)."
        },
        {
          "id": "US-003-AC03",
          "text": "Output is written to disk as an MP4 file in the configured output directory."
        },
        {
          "id": "US-003-AC04",
          "text": "Worker POSTs to gateway `/worker/done` with `{ id, url }` where `url` resolves to the MP4 artifact."
        },
        {
          "id": "US-003-AC05",
          "text": "Gateway updates job state to `succeeded` and fires the SSE event."
        },
        {
          "id": "US-003-AC06",
          "text": "`GET /v1/jobs/:id` returns `status: \"succeeded\"` and a non-empty `url` field pointing to the MP4."
        },
        {
          "id": "US-003-AC07",
          "text": "Worker propagates inference errors: job transitions to `failed` with an `error` field; no silent hangs."
        },
        {
          "id": "US-003-AC08",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Worker runs img2vid inference end-to-end",
      "description": "As a client, I want an `img2vid` job to accept an input image and produce an MP4 via `comfy_diffusion` so that I can animate a still image into a video clip.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "Worker `POST /infer` accepts `modality: \"img2vid\"` with `inputImage` (URL or base64), `modelId`, `width`, `height`, `duration`, and all required `comfy_diffusion` parameters."
        },
        {
          "id": "US-004-AC02",
          "text": "Inference runs asynchronously after `202 Accepted` using `comfy_diffusion` exclusively."
        },
        {
          "id": "US-004-AC03",
          "text": "Output is written to disk as an MP4 file in the configured output directory."
        },
        {
          "id": "US-004-AC04",
          "text": "Worker POSTs to gateway `/worker/done` with `{ id, url }` where `url` resolves to the MP4 artifact."
        },
        {
          "id": "US-004-AC05",
          "text": "Gateway updates job state to `succeeded` and fires the SSE event."
        },
        {
          "id": "US-004-AC06",
          "text": "`GET /v1/jobs/:id` returns `status: \"succeeded\"` and a non-empty `url` field pointing to the MP4."
        },
        {
          "id": "US-004-AC07",
          "text": "Worker propagates inference errors: job transitions to `failed` with an `error` field."
        },
        {
          "id": "US-004-AC08",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Playground supports video mode",
      "description": "As a client, I want the playground UI to let me select a video model, choose a video modality, set resolution and duration, submit the job, and view the resulting MP4 so that I can manually verify the video pipeline end-to-end.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "Model selector in the playground filters and displays video-capable models."
        },
        {
          "id": "US-005-AC02",
          "text": "When a video model is selected, the UI shows modality options (`txt2vid`, `img2vid`)."
        },
        {
          "id": "US-005-AC03",
          "text": "`img2vid` mode shows an image upload or URL input field."
        },
        {
          "id": "US-005-AC04",
          "text": "Width, height, and duration inputs are present and submitted as part of the job payload."
        },
        {
          "id": "US-005-AC05",
          "text": "After job completion, the playground renders a `<video>` element pointing to the returned MP4 URL."
        },
        {
          "id": "US-005-AC06",
          "text": "Visually verified in browser."
        },
        {
          "id": "US-005-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `models.config.json` schema and `loadModels()` / `findModelById()` utilities must support `txt2vid` and `img2vid` as valid modality values."
    },
    {
      "id": "FR-2",
      "description": "** Gateway job creation endpoint must validate `width`, `height` (positive integers), and `duration` (positive number) when provided; reject invalid values with `400`."
    },
    {
      "id": "FR-3",
      "description": "** Gateway must forward `width`, `height`, `duration`, and `inputImage` (for `img2vid`) to the worker `POST /infer` payload alongside existing fields (`architecture`, `components`, `modality`, flattened params)."
    },
    {
      "id": "FR-4",
      "description": "** Worker `POST /infer` Pydantic model must include `width`, `height`, `duration`, and `input_image` (optional) fields with appropriate types and validation."
    },
    {
      "id": "FR-5",
      "description": "** Worker must invoke `comfy_diffusion` video pipelines exclusively — no fallback to other libraries."
    },
    {
      "id": "FR-6",
      "description": "** All output artifacts must be MP4 files; the worker must reject or error if `comfy_diffusion` produces a non-MP4 output."
    },
    {
      "id": "FR-7",
      "description": "** The MP4 artifact URL returned in the callback and `GET /v1/jobs/:id` must be accessible via an HTTP route (gateway static route or worker static route — match whatever pattern iteration 000006/000007 established)."
    },
    {
      "id": "FR-8",
      "description": "** `GET /v1/jobs/:id/events` SSE stream must emit the terminal event for video jobs in the same format as existing modalities."
    }
  ]
}
