{
  "goals": [
    "Enable clients to submit jobs and receive results asynchronously without blocking on inference.",
    "Wire the full job state machine (`pending → running → succeeded | failed`) across the gateway in-memory store, REST endpoint, and SSE channel.",
    "Validate the gateway ↔ worker contract (job forwarding + completion callback) with a stub worker so Phase 3 can swap in real inference transparently.",
    "Maintain the fixed stack: Bun/Elysia (gateway), uv/FastAPI (worker), p-queue (concurrency 1), dotenvx."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Create a job",
      "description": "As a client application, I want to POST a job request to the gateway so that I receive a `jobId` immediately and the gateway handles the rest asynchronously.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`POST /v1/jobs` accepts a JSON body with at least `{ type: string, params: object }`."
        },
        {
          "id": "US-001-AC02",
          "text": "Gateway assigns a UUID `jobId`, stores initial state `{ id, type, params, status: \"pending\", createdAt }` in an in-memory map."
        },
        {
          "id": "US-001-AC03",
          "text": "Response is `201 Created` with body `{ jobId }` — no inference blocking."
        },
        {
          "id": "US-001-AC04",
          "text": "Requesting the same endpoint with a missing or malformed body returns `400 Bad Request` with a descriptive error message."
        },
        {
          "id": "US-001-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Queue forwards job to the stub worker",
      "description": "As a gateway, I want to enqueue the job with p-queue (concurrency 1) and forward it to the worker so that only one inference request is in flight at a time.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "p-queue is configured with `concurrency: 1`; a second job submitted while one is running waits in the queue."
        },
        {
          "id": "US-002-AC02",
          "text": "When the job reaches the front of the queue, gateway POSTs `{ id, type, params }` to the worker's `POST /infer` endpoint."
        },
        {
          "id": "US-002-AC03",
          "text": "On receipt of `202` from the worker, gateway updates job status to `\"running\"` in the in-memory map."
        },
        {
          "id": "US-002-AC04",
          "text": "If the worker is unreachable, job status is set to `\"failed\"` with an error message; no silent hang."
        },
        {
          "id": "US-002-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Get job status",
      "description": "As a client application, I want to call `GET /v1/job/:id` so that I can poll the current state of any job.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`GET /v1/jobs/:id` returns `200 OK` with a JSON body containing at least `{ id, status, createdAt, updatedAt }`."
        },
        {
          "id": "US-003-AC02",
          "text": "When the job has succeeded, the body also includes `{ url: string }` pointing to the result asset."
        },
        {
          "id": "US-003-AC03",
          "text": "When the job has failed, the body includes `{ error: string }`."
        },
        {
          "id": "US-003-AC04",
          "text": "Requesting an unknown `id` returns `404 Not Found`."
        },
        {
          "id": "US-003-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "SSE channel per job",
      "description": "As a client application, I want to open an SSE connection for a specific `jobId` so that I am notified of completion without polling.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`GET /v1/jobs/:id/events` opens an SSE stream with `Content-Type: text/event-stream`."
        },
        {
          "id": "US-004-AC02",
          "text": "When the job transitions to `\"succeeded\"` or `\"failed\"`, the gateway emits an SSE event with data `{ id, status, url? , error? }`."
        },
        {
          "id": "US-004-AC03",
          "text": "After emitting the terminal event, the gateway closes the SSE stream immediately."
        },
        {
          "id": "US-004-AC04",
          "text": "A client connecting after the job is already complete receives the terminal event immediately and the stream closes cleanly."
        },
        {
          "id": "US-004-AC05",
          "text": "Requesting an SSE stream for an unknown `id` returns `404 Not Found`."
        },
        {
          "id": "US-004-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Stub worker accepts infer requests and runs a background task",
      "description": "As a worker, I want to accept `POST /infer`, respond with `202 Accepted` immediately, and complete work in a background task so that the gateway is never blocked waiting for inference.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`POST /infer` body is validated by a Pydantic model: `{ id: str, type: str, params: dict }`."
        },
        {
          "id": "US-005-AC02",
          "text": "Response is `202 Accepted` with body `{ message: \"accepted\" }` — no blocking."
        },
        {
          "id": "US-005-AC03",
          "text": "Background task waits a short fixed delay (e.g. 2 s) to simulate inference, then POSTs `{ id, url }` to the gateway's `POST /worker/done` endpoint; `url` is a placeholder string (e.g. `\"http://localhost/assets/<id>.png\"`)."
        },
        {
          "id": "US-005-AC04",
          "text": "If the gateway callback URL is unreachable, the worker logs the error; it does not crash or retry infinitely."
        },
        {
          "id": "US-005-AC05",
          "text": "Worker health endpoint (`GET /health`) still responds `200` while a background task is running."
        },
        {
          "id": "US-005-AC06",
          "text": "Typecheck / lint passes (ruff)."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "Gateway receives completion callback and emits SSE",
      "description": "As a gateway, I want to receive `POST /worker/done` from the worker so that I can update the job map and notify the client via SSE.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "`POST /worker/done` accepts `{ id: string, url: string }`."
        },
        {
          "id": "US-006-AC02",
          "text": "Gateway updates the in-memory job entry: `status → \"succeeded\"`, `url` set, `updatedAt` refreshed."
        },
        {
          "id": "US-006-AC03",
          "text": "Gateway emits an SSE event `{ id, status: \"succeeded\", url }` to any open subscriber for that `jobId`."
        },
        {
          "id": "US-006-AC04",
          "text": "Posting with an unknown `id` returns `404 Not Found`; no state mutation occurs."
        },
        {
          "id": "US-006-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-007",
      "title": "End-to-end smoke test (manual)",
      "description": "As a developer, I want to run a documented manual smoke test so that I can verify the full job lifecycle works before merging.",
      "acceptanceCriteria": [
        {
          "id": "US-007-AC01",
          "text": "README or a `docs/smoke-test.md` file documents the exact steps: start gateway, start worker, POST a job, open SSE stream, observe completion event, GET final job status."
        },
        {
          "id": "US-007-AC02",
          "text": "Following those steps produces: `jobId` returned → status `\"pending\"` → status `\"running\"` → SSE event received → status `\"succeeded\"` with a `url` field."
        }
      ]
    },
    {
      "id": "US-008",
      "title": "Automated tests for critical paths",
      "description": "As a developer, I want automated tests covering the job lifecycle critical paths so that regressions are caught early.",
      "acceptanceCriteria": [
        {
          "id": "US-008-AC01",
          "text": "Gateway: at least one Vitest (or Bun test runner) test covers `POST /v1/jobs` → `GET /v1/jobs/:id` status progression."
        },
        {
          "id": "US-008-AC02",
          "text": "Gateway: at least one test verifies `GET /v1/jobs/:id` returns `404` for an unknown id."
        },
        {
          "id": "US-008-AC03",
          "text": "Worker: at least one pytest test covers `POST /infer` returning `202` and the Pydantic validation rejecting a bad body."
        },
        {
          "id": "US-008-AC04",
          "text": "All tests pass with `bun test` (gateway) and `uv run pytest` (worker)."
        },
        {
          "id": "US-008-AC05",
          "text": "Typecheck / lint passes for test files."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** Gateway exposes `POST /v1/jobs`, `GET /v1/jobs/:id`, `GET /v1/jobs/:id/events`, and `POST /worker/done`."
    },
    {
      "id": "FR-2",
      "description": "** Job state machine has exactly four states: `pending`, `running`, `succeeded`, `failed`; transitions are one-way."
    },
    {
      "id": "FR-3",
      "description": "** In-memory job store is a `Map<string, Job>` (or equivalent); no external database in this phase."
    },
    {
      "id": "FR-4",
      "description": "** p-queue instance is configured with `concurrency: 1` and shared across the gateway process lifetime."
    },
    {
      "id": "FR-5",
      "description": "** Worker callback URL (gateway base URL) is read from an environment variable (e.g. `GATEWAY_URL`), not hardcoded."
    },
    {
      "id": "FR-6",
      "description": "** Gateway base URL for outbound worker requests is read from an environment variable (e.g. `WORKER_URL`), not hardcoded."
    },
    {
      "id": "FR-7",
      "description": "** Stub worker background task uses a fixed simulated delay (configurable via env var, default 2 s)."
    },
    {
      "id": "FR-8",
      "description": "** SSE stream auto-closes immediately after a terminal event (`succeeded` or `failed`) is emitted; connections are also cleaned up if the client disconnects early."
    }
  ]
}
